# Directory-Scoped Config

Color scheme per folder. Config inheritance that walks up the directory tree. A workspace in ~/code/rust gets different colors than ~/code/python automatically.

tags: config, ux, workspaces
ref: #1556
