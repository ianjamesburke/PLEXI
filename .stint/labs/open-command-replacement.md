# Opt-in `open` Command Replacement

`open foo.txt` opens in Plexi instead of the system default. Opt-in shell alias that routes file opens through Plexi's file viewer/editor.

tags: cli, shell, ux
ref: #1558
