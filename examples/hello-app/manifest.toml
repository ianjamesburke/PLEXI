[app]
id = "hello-app"
name = "Hello App"
entry = "hello_app.py"
version = "0.2.0"
description = "Example Plexi app — demos rect/text/list plus image, video_thumbnail, and file_grid"

[app.capabilities]
file_types = ["hello"]
filesystem = "read_only"
# keybinding = "cmd+shift+h"  # optional
