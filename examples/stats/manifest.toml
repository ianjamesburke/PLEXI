schema_version = 1

[app]
id = "stats"
type = "app"
name = "Plexi Stats"
version = "0.1.0"
description = "Focus-flow DAG visualization showing context transitions, dominant areas, and switch patterns from Plexi focus events."
entry = "stats.py"

[app.capabilities]
capabilities = []

[launch]
layout_hint = { side = "right", split = 0.4 }
