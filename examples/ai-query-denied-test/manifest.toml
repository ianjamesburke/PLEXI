schema_version = 1

[app]
id = "ai-query-denied-test"
type = "app"
name = "AI Query Denied Test"
version = "0.1.0"
description = "POC for #284 — proves the ai.query capability gate. This app does NOT declare ai.query, so every call returns capability_denied."
entry = "ai_query_denied_test.py"
default_notification_scope = "context"

[app.capabilities]
capabilities = []

[launch]
background = false
