[app]
id = "screen-time"
name = "Screen Time"
version = "0.1.0"
description = "Pie chart of macOS app usage from the last 7 days."
entry = "screen_time.py"

[launch]
layout_hint = { side = "right", split = 0.5 }
