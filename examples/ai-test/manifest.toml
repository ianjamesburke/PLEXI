schema_version = 1

[app]
id = "ai-test"
type = "app"
name = "AI Test"
entry = "main.py"
version = "0.1.0"
description = "Minimal ai_query smoke test"
watch = true

[app.capabilities]
capabilities = ["ai.query"]

[launch]
layout_hint = { side = "right", split = 0.5 }
