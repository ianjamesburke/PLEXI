schema_version = 1

[app]
id = "video-editor"
type = "app"
name = "Video Editor"
version = "0.1.0"
description = "Parallax — visual POC for a non-linear video editor. No actual decoding; demonstrates the editor chrome, timeline, and panel layout."
entry = "video_editor.py"

[app.capabilities]
capabilities = []

[launch]
layout_hint = { side = "right", split = 0.6 }
