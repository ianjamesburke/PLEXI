[app]
protocol_version = 2
id = "backlog-triage"
name = "Backlog Triage"
entry = "backlog_triage.py"
version = "0.2.0"
description = "Keyboard-driven review of backlog notes — archive, trash, or read"

[app.launch]
mode = "fullscreen"

[app.capabilities]
file_types = []
terminal_write = false
filesystem = "write"
