[app]
id = "github-tree"
name = "GitHub Tree"
version = "0.2.0"
description = "Browse the current repo's tree. Auth via the gh CLI."
entry = "github_tree.py"

[app.capabilities]
capabilities = []

[launch]
layout_hint = { side = "above", split = 0.5 }
