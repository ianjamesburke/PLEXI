schema_version = 1

[app]
id = "logs"
type = "app"
name = "Logs"
version = "0.2.0"
description = "Live log drain with level filters and substring search."
entry = "logs.py"
watch = true

[app.capabilities]
capabilities = ["fs.read", "timer"]

[launch]
layout_hint = { side = "right", split = 0.25 }
