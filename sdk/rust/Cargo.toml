[package]
name = "plexi-sdk"
version = "0.1.0"
edition = "2021"
description = "Rust SDK for building Plexi external apps"
license = "MIT"
repository = "https://github.com/ianjamesburke/plexi"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
