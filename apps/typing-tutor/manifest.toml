schema_version = 1

[app]
id = "typing-tutor"
type = "app"
name = "Typing Tutor"
version = "0.1.0"
description = "Touch typing trainer — 10 progressive levels with star-gated unlocks."
entry = "main.py"

[app.capabilities]
capabilities = []

[launch]
keyboard_capture = true
layout_hint = { side = "right", split = 0.6 }
