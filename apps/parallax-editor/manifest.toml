schema_version = 1

[app]
id = "parallax-editor"
type = "app"
name = "Parallax Editor"
version = "0.1.0"
description = "GUI for parallax video production — wraps parallax produce and parallax plan"
entry = "main.py"

[launch]
layout_hint = { side = "right", split = 0.6 }
