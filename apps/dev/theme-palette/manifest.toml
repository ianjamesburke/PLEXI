schema_version = 1

[app]
id = "theme-palette"
type = "app"
name = "Theme Palette"
entry = "main.py"
version = "0.1.0"
description = "POC for SDK theme/color namespace — AppPalette light/dark tokens (#1636)."
watch = true

[app.capabilities]
capabilities = []

[launch]
