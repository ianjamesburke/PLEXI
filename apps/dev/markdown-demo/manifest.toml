schema_version = 1

[app]
id = "markdown-demo"
type = "app"
name = "Markdown Demo"
entry = "main.py"
version = "0.1.0"
description = "Static markdown sample for visual styling regression (code blocks, inline code, blockquotes, links)"
watch = true

[app.capabilities]
capabilities = []

[runtime]
python_compat = true
