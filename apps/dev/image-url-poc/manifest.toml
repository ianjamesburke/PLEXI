schema_version = 1

[app]
id = "image-url-poc"
type = "app"
name = "Image URL POC"
entry = "main.py"
version = "0.1.0"
description = "POC for remote URL image loading (#1353)"
watch = true

[app.capabilities]
capabilities = ["net.http"]

[launch]
