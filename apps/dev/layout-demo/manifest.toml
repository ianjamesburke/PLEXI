schema_version = 1

[app]
id = "layout-demo"
type = "app"
name = "Layout Demo"
entry = "app.py"
version = "0.1.0"
description = "POC for the declarative layout model (issue #1080)."
watch = true

[app.capabilities]
capabilities = []

[launch]
