schema_version = 1

[app]
id = "gh-projects"
type = "app"
name = "GitHub Projects"
entry = "main.py"
version = "0.1.0"
description = "Read-only GitHub Projects Kanban board — navigable with arrow keys"
watch = true

[app.capabilities]
capabilities = ["fs.read"]

[launch]
