schema_version = 1

[app]
id = "csv_viewer"
type = "app"
name = "CSV Viewer"
version = "0.1.0"
description = "Browse and inspect CSV files through host-scoped file listing."
entry = "csv_viewer.py"

[runtime]
python_compat = true

[app.capabilities]
capabilities = ["fs.read"]

[launch]

[marketplace]
visibility = "public"
price = "free"
publisher = "plexi"
