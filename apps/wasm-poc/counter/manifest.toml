schema_version = 1

[app]
id = "com.plexi.counter"
type = "wasm"
name = "Counter"
entry = "target/wasm32-wasip1/release/counter.wasm"
version = "0.1.0"
description = "Counter POC for typed WASM UI actions."

[app.capabilities]
capabilities = []

[launch]
