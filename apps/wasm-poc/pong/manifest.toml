schema_version = "2"
id = "com.plexi.pong"
version = "0.1.0"
name = "Pong"
description = "Pong — surface-node POC demonstrating direct GPU rendering inside a Plexi pane."

[runtime]
target = "wasm32-wasip2"
execution = "local"
python_compat = false

[capabilities]
required = []
optional = []

[payment]
model = "free"
