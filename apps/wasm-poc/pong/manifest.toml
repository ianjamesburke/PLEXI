schema_version = 1

[app]
id = "com.plexi.pong"
type = "wasm"
name = "Pong"
entry = "target/wasm32-wasip2/release/pong.wasm"
version = "0.1.0"
description = "GPU surface-node POC demonstrating direct rendering inside a Plexi pane."

[runtime]
target = "wasm32-wasip2"
execution = "local"
python_compat = false

[app.capabilities]
capabilities = []

[app.capabilities.wasm]
required = []
optional = []
