schema_version = 1

[app]
id = "com.plexi.sysmon"
type = "wasm"
name = "System Monitor"
entry = "../../../target/wasm32-wasip1/release/sysmon.wasm"
version = "0.1.0"
description = "CPU, memory, disk, and network stats POC for the Plexi WASM runtime."

[app.capabilities]
capabilities = []

[app.capabilities.wasm]
required = ["state:read-write"]
optional = []

[launch]
