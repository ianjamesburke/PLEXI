schema_version = 1

[app]
id = "com.plexi.python-shim"
type = "wasm"
name = "Python Shim"
entry = "target/wasm32-wasip2/release/python_shim.wasm"
version = "0.1.0"
description = "CPython lifecycle shim boundary POC for SDK v3 JSON bridge."

[app.capabilities]
capabilities = []

[app.capabilities.wasm]
required = ["state:read-write"]
optional = []

[launch]
