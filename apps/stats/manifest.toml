schema_version = 1

[app]
id = "stats"
type = "app"
name = "Stats"
version = "0.2.0"
description = "Activity dashboard: focus level, time, streak, top projects, and a 24h heatmap from Plexi focus events."
entry = "stats.py"

[runtime]
python_compat = true

[app.capabilities]
capabilities = ["timer"]

[launch]

# Uncomment to publish this app to the marketplace:
# [marketplace]
# visibility = "public"   # public | unlisted | private
# price = "free"          # "free", or a price like "4.99"
# publisher = "plexi"     # first-party Plexi app
