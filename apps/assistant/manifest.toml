schema_version = 1

[app]
id = "assistant"
type = "app"
name = "Assistant"
version = "0.1.0"
description = "AI chat assistant powered by Plexi's ai.query broker."
entry = "assistant.py"

[app.capabilities]
capabilities = ["ai.query"]

[launch]
