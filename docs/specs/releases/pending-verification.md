# Pending Human Verification

Items merged to alpha that haven't yet been visually/manually confirmed by the user. As the user works through them, tick the box. If a check fails, the orchestrator runs `git revert <sha>` on alpha and files a regression issue.

Newest at the top. Completed items get archived to `DEV_LOG.md` as part of the next release-level verification gate.

## POC test app gaps (retroactive — pre-rule features)

Items that shipped before the "every user-visible feature ships a POC app" rule was codified. Each needs a small `examples/<feature>-test/` follow-up so the user can verify without manually setting up scaffolding:

- [ ] **#316 lifecycle pill** — needs `examples/lifecycle-tester/` with one-click buttons: "crash now" (raise), "hang now" (infinite loop), "spam malformed JSON", "exit cleanly".
- [ ] **#287 directory-scoped registry** — needs `examples/.plexi-registry-tester/` (or a docs page in `docs/`) showing how to drop a manifest into a workspace's `.plexi/apps/` and have it appear. Probably just docs + a sample manifest, not a runtime app.
- [x] **#322 workspace secret routing** — POC delivered: `examples/secrets-routing-tester/` shipped via PR #330 / `bf229ff`. Two declared canonical secrets, Fetch buttons, masked value preview, workspace UUID footer.

## Pending

- [ ] **2026-04-26 — PR #366 (`aa91270`) — Agent Workspace UX layer (#349).** Open `Plexi Alpha.app` from a git repo. Palette → "New Agent Workspace…". Modal opens with CLI dropdown (installed CLIs enabled; uninstalled show "(not installed)" and are unselectable), repo path field pre-filled with current repo, task textarea. Pick an installed CLI, accept defaults, press ⌘↩ — pane spawns within 2s; header shows `<CLI> · <branch> · <task>`. Status badge transitions Idle → Thinking → Writing → Done as the CLI runs. Sidebar shows modified files within ~2s of `touch`-ing any file in the worktree. When Done, click "Ready to merge" — original repo's main gets the merge commit. Deliberately conflicting branches → conflict notification card with file list. Second Agent Workspace from the same repo pre-selects last-used CLI. Existing flat palette commands ("New Agent Workspace: Claude Code/Codex/Gemini CLI") still spawn directly without the modal.
- [ ] **2026-04-26 — PR #365 (`35efce6`) — GUI↔terminal media bridge (#79).** Open `Plexi Alpha.app`. Drop an mp4 into the file browser; double-click. `video-player` opens (not QuickTime). Same for an mp3 → `audio-player`. Run `audio-bridge-test`. Linked terminal pane appears. Press `r` — mic captures, waveform paints, `ffmpeg -f avfoundation -i :0 -t 5 -ar 48000 capture.wav` echoes into the terminal. Press stop — capture ends; "# capture stopped" in terminal. In `video-player`, click Play — equivalent `ffplay <path>` echoes into linked terminal. Drop a `.tiff` (unrecognized) → still falls through to system `open`.
- [ ] **2026-04-26 — PR #364 (`940890d`) — Hot reload (#83).** Copy `examples/hot-reload-test/` into a test workspace's `.plexi/apps/`. Launch Plexi Alpha from that workspace. Spawn `hot-reload-test` — counter increments per frame. Edit the source file's `DISPLAY_STRING` constant; save. App reloads within ~500ms; counter resets; new constant displayed. With a non-watch app focused (e.g. snake), press Cmd+Option+R — app reloads, game state resets. Spawn `hot-reload-test` from `~/.plexi-alpha/apps/` (global) — editing source does NOT auto-reload (workspace-local only). Close a watching pane — `~/.plexi-alpha/plexi.log` shows the unwatch line. Bonus: a subprocess that ignores Shutdown no longer hangs the host on close (try_wait poll loop replaces the broken Drop path).
- [ ] **2026-04-26 — PR #363 (`1c6d9d3`) — Rich notification panel (#74).** Open `Plexi Alpha.app`. Run `notification-tester`. Press `p` — inline-image notification appears with a 64×64 gradient PNG above the body. Press `q` — image + 3 structured choices (Sidebar / Fullwidth / Retry); pressing `1`/`2`/`n` (or clicking) routes the matching payload through. Press `t` — oversized 100KB image triggers the host's "image too large" placeholder badge. Existing free-text actions (`m`/`c`/`i`/`r` etc.) still work. Cmd+]/Cmd+[ cycle through queued notifications; priority queue + BlockedOnUser pinning preserved.
- [ ] **2026-04-26 — PR #362 (`1c0dcf6`) — Canvas Terminal Binding Primitives 5 of 7 (#78).** Open `Plexi Alpha.app`. Run `canvas-terminal-bindings-test`. A linked terminal pane appears next to the Canvas app. Click "Run ls" — terminal echoes the command and shows output. Click "Insert /tmp" — `/tmp` appears at the terminal cursor. Click "Preview rm -rf .git" — modal shows the would-run command + cwd, doesn't execute. Click "Open ~/Desktop" — file-browser pane opens at ~/Desktop. App without `terminal.bindings` capability calling RunInLinkedTerminal gets clear capability-denied response (or fire-and-forget drop with host log for the non-blocking primitives). Streaming + completions buttons are deferred to follow-ups #358/#359.
- [ ] **2026-04-26 — PR #357 (`ffb659a`) — CLI wrapper registry substrate (#321).** Run `plexi-alpha descriptor probe gh` (assuming `gh` is installed). Output shows the registry's gh descriptor with `(via registry)` indicator. Run `plexi-alpha descriptor probe gh --no-registry` — falls through to "no descriptor available" message. `plexi-alpha registry watch` walks gh/cargo/npm; reports installed version vs registry (likely `[STALE]` for all three since seeded versions are intentionally older). Drop a hand-edited `~/.plexi-alpha/registry/gh/latest.json` with a different name field — re-run probe; user override wins.
- [ ] **2026-04-26 — PR #353 (`f96f545`) — AVFoundation backing (#346).** Open `Plexi Alpha.app`. Run `video-player`. Hardcode (or supply via `PLEXI_VIDEO_PATH` env) a path to a real H.264 MP4. Play — bytes-per-frame ≈ width × height × 4. Frame counter increments at native fps. Pause stops frames; Resume continues. Seek mid-clip jumps. 60s+ clip start to finish, no panics in `~/.plexi-alpha/plexi.log`. Mock path still works: `PLEXI_VIDEO=mock://gradient plexi-alpha` runs the mock unchanged.
- [ ] **2026-04-26 — PR #352 (`c04fb76`) — `--plexi` descriptor spec (#188).** Run `plexi-alpha descriptor probe python3 examples/plexi-descriptor-demo/plexi_descriptor_demo.py`. Probe prints a parsed summary (name, version, description, command count, first 3 commands). Hand-edit the demo descriptor to add an unknown top-level field — re-probe shows clear error naming the field. Hand-edit to remove a required field (e.g. `plexi_version`) — clear error. Schema lives at `schemas/plexi-descriptor-schema.json` for IDE/author tooling. Renderer/consumer that builds auto-UIs from descriptors arrives in #78.
- [ ] **2026-04-26 — PR #351 (`e5d6b0d`) — Agent Workspace substrate (#348).** Open `Plexi Alpha.app` from a git repo. Palette → "New Agent Workspace: Claude Code" (or whichever CLI is installed). Pane spawns within 2s in an auto-created git worktree (`<repo>/.git/worktrees/plexi-<id>`); header shows CLI + branch (`plexi/agent-<slug>-<short-hash>`). Close pane → `git worktree list` no longer shows the plexi worktree (branch stays for review). Quit and reopen Plexi — pane restores; CLI relaunches in the existing worktree. From a directory without `.git/`: pane shows clear error in scrollback. Uninstalled CLI: spawn fails with clear "binary not found" in scrollback. UX chrome (modal + status heuristics + diff sidebar + merge button) is #349.
- [ ] **2026-04-26 — PR #350 (`5a6b9bd`) — video substrate (#345).** Open `Plexi Alpha.app` with `PLEXI_VIDEO=mock://gradient` set in env. Run `video-player`. Frame counter increments at the configured fps (320×180 @ 30 fps); state shows Playing/Paused/Seeking. Buttons: Play/Pause toggle works; Seek 5s back/forward updates position. Bare `plexi-alpha` (no `PLEXI_VIDEO`) — production stub returns NotImplemented; `video-player` shows clear error in scrollback (real AVFoundation is #346). Existing apps without `video.playback` capability still launch normally.
- [ ] **2026-04-26 — PR #347 (`12e0734`) — agent roster + inter-agent pipes (#286).** Open `Plexi Alpha.app`. Run `agent-coordinator` and `agent-worker` in two panes. In coordinator, type "ask the worker what 2+2 is". Coordinator's history shows: list-agents → pipe-open to worker → worker reply → final answer. Worker's history shows the incoming pipe message and its reply. Spawn a third agent (e.g. `agent-tester`) without `agents.list`; calling `agent_roster()` from it returns an empty list (no error). Existing `agent-tester` runs normally — no regression.
- [ ] **2026-04-26 — PR #344 (`a2941a0`) — CoreMIDI I/O (#320).** Open `Plexi Alpha.app`. Run `midi-test`. With no MIDI device connected, input/output columns show empty or just IAC Driver. Connect a USB MIDI controller — ports appear after pressing `r` to refresh. Press `o` to open first input; press keys/pads on controller — events stream to log column with hex bytes + decoded form (NoteOn ch=N note=M vel=V). Connect a hardware synth (e.g. Take 5) as MIDI output: press `n`/`m` — synth produces sound and stops. Press `k` — sequencer locked to external clock should pulse. App without `midi.out` calling `send_midi` gets clear capability-denied response.
- [ ] **2026-04-26 — PR #342 (`0df3e1e`) — Agent-as-app part 2 host integration (#338).** Open `Plexi Alpha.app`. Run `agent-tester`. Conversation UI opens (not a draw canvas). Type a question — agent replies via brokered iq.query. Continue the conversation — history preserved across turns. Drop `examples/agent-tester/manifest.toml` into `~/work-test/.plexi/agents/agent-tester/`; launch from there — agent appears in palette under workspace agents. Existing legacy IQ pane (Cmd+I) still works in-process (no regression). Existing `type = "app"` apps render normally.
- [ ] **2026-04-26 — PR #340 (`dfefd08`) — CoreAudio capture + enumeration (#277).** Open `Plexi Alpha.app`. Confirm host launches without panic on cpal init. Capture path is reachable on the wire (`AudioCapture` DrawCommand) but no SDK helper or POC recorder app yet — that's follow-up #341. macOS first-run: a "Plexi Alpha would like to access the microphone" prompt appears once when an app actually requests capture. Existing apps without audio capability still launch normally.
- [ ] **2026-04-26 — PR #337 (`dfd2405`) — Agent-as-app part 1 wire/schema (#285).** No user-visible verification — wire/schema-only PR. Sanity check: existing apps still launch normally (every example manifest gained a required `type = "app"` field). `cargo test --bin plexi` reports 114 passing. Full user-facing verification lives with #338 (host integration + SDK Agent class + agent-tester POC).
- [ ] **2026-04-26 — PR #336 (`6a51d96`) — iq.query brokered capability (#284).** Open `Plexi Alpha.app`. Run `iq-query-test`. Type "What is 2+2?", click "Low (Haiku)" — response within seconds, tokens shown. Click "Medium (Sonnet)" with same prompt — different model, different reply. Run `iq-query-denied-test` — capability-denied error shown. After a few queries, `cat ~/.plexi-alpha/agent-ledger.jsonl | tail` shows entries with model, tokens_in, tokens_out, cost_cents. No raw API key in `~/.plexi-alpha/plexi.log` or any app's stderr.
- [ ] **2026-04-26 — PR #335 (`b737781`) — #308 Phase 3 Installer App UI.** On a fresh alpha install, `app-store` appears in the command palette automatically (came from updated core pack). Open it. Three tabs: Browse / Install by URL / Installed. Browse tab: shows clear "registry unavailable" message (default registry repo `ianjamesburke/plexi-apps-registry` doesn't exist yet). Install by URL: paste a real public git URL or `github:user/repo[@ref]` — capability preview modal appears (i = install, c = cancel) — confirm and the app installs and joins the palette. Installed tab: lists installed apps with version + scope + Update / Uninstall actions. Header "Export current setup" writes a valid `pack.toml`; `plexi-alpha install --pack <that-path>` reproduces the set on a fresh apps dir.
- [ ] **2026-04-26 — PR #334 (`e1eef02`) — #308 Phase 2 App Package Manager.** On a fresh alpha install (`rm -rf ~/.plexi-alpha/apps && just install-alpha`), launch the host — apps dir auto-populates from the bundled core pack. Run `plexi-alpha list` — see installed apps with versions/scopes. Test git-based install: create a small public repo with a valid manifest (`schema_version = 1`), run `plexi-alpha install git+https://...` or `plexi-alpha install github:user/repo` — app lands in `~/.plexi-alpha/apps/<id>/`. `plexi-alpha install` of a manifest with `schema_version = 99` errors clearly. `plexi-alpha uninstall <id> --yes` removes it; subsequent `list` confirms. `plexi-alpha update <id>` runs `git fetch && git pull` and reports outcome. Open `workspace-config-tester` — Card now shows scanned installed apps live (press `r` to refresh after installing).
- [ ] **2026-04-26 — PR #333 (`88c7c79`) — #308 Phase 1 workspace model.** Drop `[log]\nlevel = "debug"\n` into a workspace's `.plexi/config.toml`. Launch `plexi-alpha /path/to/that/workspace` and run `workspace-config-tester` — `log.level` row shows `debug`. Bare `plexi-alpha` from CWD inside same workspace = same result. From a terminal inside the workspace, `cd /tmp` — pane name bar shows amber `↗ outside workspace` badge. After fresh `plexi-alpha workspace init`, `.plexi/.gitignore` exists with `secrets.toml` + `cache/` entries; re-running init does NOT overwrite user-edited gitignore.
- [ ] **2026-04-25 — PR #332 (`8dbdf61`) — clipboard + paste + selectable text (#200, #146).** Open `Plexi Alpha.app`, run `clipboard-test`. Click the copy button — paste into TextEdit/another app and the timestamp appears. Copy text from outside Plexi, focus `clipboard-test`, Cmd+V — status line updates with the pasted text. Drag-select within the Lorem ipsum block, Cmd+C, paste elsewhere — selected text appears. Existing apps (snake/todo/etc.) still render normally with `selectable=false` default. Note: `widgets/text_area.py` had a pre-existing wire payload bug (used `fill` instead of `color`, missed `max_width`/`elide`) that #332 fixed inline — confirm any TextArea-using apps still render correctly.
- [ ] **2026-04-25 — PR #330 (`bf229ff`) — secrets-routing-tester POC** (retroactive POC for #322). Verify alongside the #322 entry below: `plexi-alpha workspace init` in two dirs, set different routes, run `secrets-routing-tester` in each, confirm Fetch returns different masked values per workspace. Fallback=false + missing route shows clear missing-secret state. Logs do NOT contain full secret values.
- [ ] **2026-04-25 — PR #329 (`cbbc35b`) — Cmd+N/Cmd+Shift+N + Shift+Cmd+H/J/K/L (#306).** Open `Plexi Alpha.app`. Cmd+N — focused pane splits to the right; new pane mirrors the focused pane's type. Cmd+Shift+N — splits below. Set up 6 panes in a complex layout; Shift+Cmd+H/J/K/L from each lands on the geometrically-correct neighbor. Splitting an app pane (e.g. snake) produces a fresh instance of the same app. Quit and reopen — layout (including post-split panes) restored. Existing Cmd+D / Cmd+Shift+D split shortcuts and sidebar `+` still work.
- [ ] **2026-04-25 — PR #328 (`7945250`) — workspace secret routing (#322).** In two dirs (`~/work-test/`, `~/personal-test/`): `plexi-alpha workspace init`. Each `.plexi/workspace.toml` has a unique UUID, empty `secrets.toml`. `plexi-alpha secret set test` in each (different values). `plexi-alpha secret list` in each shows the right entries (workspace + user namespaces). Route the same canonical name in both `.plexi/secrets.toml` to different friendly names; an app declaring that secret returns different values per workspace. `fallback = false` + missing route → clear in-pane error. `fallback = true` + user-scope key → app reads through fallback (log line confirms). **POC test app for this is queued separately** in the "POC test app gaps" section above.
- [ ] **2026-04-25 — PR #327 (`e3c7f06`) — directory-scoped registry (#287).** Create `~/work-test/.plexi/apps/test-app/manifest.toml` with a valid manifest. Launch Plexi Alpha from `~/work-test/`. test-app appears in command palette. Same id present globally — local wins; an `info` line `shadows global entry from …` appears in `~/.plexi-alpha/plexi.log`. Launch from `~/`; local app gone, global apps still work. Drop `.plexi/agents/test-agent/manifest.toml`; agent is discoverable.
- [ ] **2026-04-25 — PR #326 (`39e2f5d`) — TextInput primitive (#283).** Open `Plexi Alpha.app`, run the `backlog` app, press `n`. A "New backlog item" overlay appears with a text input. Type a name, press Enter — a new markdown stub appears in the list. Resize the pane while typing — content + cursor survive. Spawn a second backlog pane; each pane's input buffer is independent.
- [ ] **2026-04-25 — PR #325 (`32695ed`) — observable app lifecycle pill (#316).** Open `Plexi Alpha.app` and run snake — no pill should be visible (Running is invisible). Then drop in a crashy app: copy `examples/snake/snake.py` into a new app dir, replace its body with `raise RuntimeError("boom")`, install with `just install-alpha`, spawn it. Within 1s a red `crashed` pill appears top-right; click it to toggle the stderr traceback overlay. With a healthy app running, `kill -9 $(pgrep -f <app>.py)` from a terminal — pill goes red within 1s, host stays alive. (Hung + protocol_error states are best-effort to verify; skip if low-value.)
- [ ] **2026-04-24 — PR #315 (`9085ca8`) — bounded paint + Scrollable + chrome backdrops.** Open commit-graph at a repo with a 50+ commit week. Scroll. No green pixel bleed under FooterKeys. AppBar in any app is fully opaque (try wikipedia or commit-graph). Resize a pane while content renders; no paint outside pane bounds. (Backfilled — discovered during 2026-04-25 audit; #314 was orphan-open until then.)

The commitgraph app. Maybe it needs to be forcefully updated, but it doesn't scroll. 

- [ ] **2026-04-24 — `7c30629` — single-source pane_rect refactor.** Open the geometry-test app (or any 4-deep nested split). Every renderer call site shows correct `pane_rect`. No drift on parent resize. (Backfilled — discovered during 2026-04-25 audit; #317 was orphan-open until then.)
!!! I don't see a geometry test app. 

- [x] **2026-04-23 — PR #313 (`fedfacc`) — host-measured text layout (Badge/KeyChip/MeasureText).** Verify badge pills are correctly sized in commit-graph and elsewhere. Key chips show as styled chips (not plain text). Truncation cuts at the right character count. (Backfilled — #312 was orphan-open until 2026-04-25.)
- [x] **2026-04-25 — PR #324 (`410cb02`) — orchestration contract update.** Docs-only. Sanity check: open `Plexi Alpha.app`, launch any pane, run an existing app (snake/todo/wikipedia), confirm it renders normally.
- [x] **2026-04-25 — PR #323 (`2c309a0`) — v3.1–v3.5 roadmap + orchestration spec.** Docs-only. Sanity check: same as above.

## Done
*Items move here once verified, then to DEV_LOG at release-gate time.*
