# v3.1 — Foundation: Geometry, Lifecycle, Layout

**Theme:** rendering substrate. Nothing else builds without this.
**Headline:** #312 host-measured text layout primitives.
**Milestone:** [v3.1](https://github.com/ianjamesburke/PLEXI/milestone/1)

## Issues
- [x] #312 — Host-measured text layout primitives + SDK migration (P1, headline) — shipped via PR #313 / `fedfacc`
- [x] #314 — Bounded paint regions + Scrollable SDK primitive (P1) — shipped via PR #315 / `9085ca8` (+ `6ec880c` fix)
- [x] #316 — Observable app lifecycle — surface every failure mode in-pane (P1) — shipped via PR #325 / `32695ed`
- [x] #317 — Single-source-of-geometry — explicit `pane_rect` everywhere (P2) — shipped via `7c30629`
- [x] #283 — TextInput primitive (P2) — shipped via PR #326 / `39e2f5d`

## Release-level human verification

Run after all milestone PRs are merged and individually verified.

- [ ] Open every existing app one by one. Each renders identical to pre-v3.1 (visual diff, not just absence of crash).
- [ ] Create a 4-deep nested split. Geometry stays correct at every nesting level — no overlap, no gaps, no visual drift on parent resize.
- [ ] In a Scrollable container, scroll a long buffer past 10k lines. Clipping is exact at the bounds; no overdraw outside the paint region.
- [ ] Force-kill an app process externally (`kill -9 <pid>`). Pane shows clear in-pane failure state with restart affordance — host stays alive.
- [ ] Force-OOM an app (synthetic). Lifecycle state surfaces in-pane within 2s.
- [ ] Resize a pane while a TextInput inside it has focus. Cursor stays positioned correctly; layout reflows; no input loss.
- [ ] Open the geometry-test app (added as part of #317). Every renderer call site shows correct `pane_rect` in debug overlay.

## Breaks if (rolled up from PRs)
*Filled in as PRs merge.*
