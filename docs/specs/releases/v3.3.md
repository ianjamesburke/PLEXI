# v3.3 — Agents as First-Class Citizens

**Theme:** PGAP for agents. The protocol pays off here.
**Headline:** #285 Agent-as-app — PGAP subprocess agents with manifests.
**Milestone:** [v3.3](https://github.com/ianjamesburke/PLEXI/milestone/3)

## Issues
- [x] #285 — Agent-as-app — PGAP subprocess agents with manifests (P1, headline) — both parts shipped
  - [x] Part 1 — manifest `type` field + protocol variants + broker widening — shipped via PR #337 / `dfd2405`
  - [x] Part 2 — host integration (subprocess-backed Pane::Agent + SDK Agent class + POC `examples/agent-tester/`) — shipped via PR #342 / `0df3e1e`
  - [ ] Cleanup — delete legacy agent_turn.rs / in-process turn loop — tracked in #339 (defer until part 2 bakes)
- [x] #284 — `iq.query` — Plexi IQ as brokered capability for apps and agents (P1) — shipped via PR #336 / `6a51d96`
- [x] #286 — Agent roster + inter-agent pipes — agents aware of and callable by each other (P2) — shipped via PR #347 / `12e0734`
- [ ] #58 — Agent Workspace pane: parallel AI coding with git worktree isolation (P2)

## Release-level human verification

- [ ] Drop an agent manifest in `.plexi/agents/research.toml`. Launch from a pane. Agent runs as PGAP subprocess. Log shows `app::research` lifecycle entries.
- [ ] Agent calls `iq.query(...)`. Verify log entry showing capability gate fired and brokered call happened. Result returned to agent.
- [ ] Agent A is configured to call Agent B over inter-agent pipe. Trigger A; verify B receives the call and both log the exchange.
- [ ] Agent in `~/work-test/` reads `OPENAI_API_KEY` and gets the work-routed key (verifies v3.2 scoping carries to agents).
- [ ] Open Agent Workspace pane. Spawn 3 agents in parallel, each in its own worktree. Each runs independently; output streams to its sub-pane; killing one doesn't affect the others.
- [ ] Agent crashes mid-task. In-pane lifecycle state shows failure with restart affordance (carries v3.1 lifecycle work).
- [ ] Agent without `iq.query` capability in its manifest tries to call it. Capability gate denies the call, error surfaces in-pane.

## Breaks if (rolled up from PRs)
*Filled in as PRs merge.*
