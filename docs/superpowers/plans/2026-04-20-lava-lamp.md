# Lava Lamp Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a lava lamp Plexi app with buoyancy-driven blobs and fake-metaball visual merging using only `DrawCommand::Circle`.

**Architecture:** Single Python file following the `examples/balls/balls.py` pattern. `Blob` objects carry temperature state that drives buoyancy. Per-frame, nearby blob pairs render translucent "bridge" circles between them to fake merging.

**Tech Stack:** Python 3.11+, Plexi SDK (`plexi_sdk` from `sdk/python/`), `ctx.circle` / `ctx.clear` / `ctx.text` / `ctx.emit.schedule_render`.

---

### Task 1: Scaffold the app directory and manifest

**Files:**
- Create: `examples/lava-lamp/manifest.toml`
- Create: `examples/lava-lamp/lava_lamp.py` (skeleton only)

- [ ] **Step 1: Create the directory and manifest**

```bash
mkdir -p /Users/ianburke/Documents/GitHub/PLEXI/.claude/worktrees/v3/examples/lava-lamp
```

Create `examples/lava-lamp/manifest.toml`:

```toml
[app]
id = "lava-lamp"
name = "Lava Lamp"
version = "0.1.0"
description = "Fluid blob simulation with fake-metaball blending — demonstrates DrawCommand::Circle layering."
entry = "lava_lamp.py"

[app.capabilities]
capabilities = []

[launch]
layout_hint = { side = "right", split = 0.35 }
```

- [ ] **Step 2: Create the skeleton app**

Create `examples/lava-lamp/lava_lamp.py`:

```python
#!/usr/bin/env python3
"""Lava Lamp — buoyancy-driven blobs with fake-metaball blending.

Blobs rise when hot, sink when cool. Nearby blobs render translucent bridge
circles between them to simulate the characteristic merging-and-splitting look.
"""
from __future__ import annotations

import math
import random
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '../../sdk/python'))

from plexi_sdk import App, RenderContext, dim

if __name__ == "__main__":
    pass  # replaced in Task 4
```

- [ ] **Step 3: Verify it is executable and readable**

```bash
chmod +x /Users/ianburke/Documents/GitHub/PLEXI/.claude/worktrees/v3/examples/lava-lamp/lava_lamp.py
python3 /Users/ianburke/Documents/GitHub/PLEXI/.claude/worktrees/v3/examples/lava-lamp/lava_lamp.py
```

Expected: exits immediately (no PGAP handshake yet), no import errors.

- [ ] **Step 4: Commit**

```bash
cd /Users/ianburke/Documents/GitHub/PLEXI/.claude/worktrees/v3
git add examples/lava-lamp/
git commit -m "feat: scaffold lava-lamp app directory and manifest"
```

---

### Task 2: Implement the Blob class and physics model

**Files:**
- Modify: `examples/lava-lamp/lava_lamp.py`

- [ ] **Step 1: Add constants and the Blob class**

Replace the body of `lava_lamp.py` (after imports, before `if __name__`) with:

```python
# ── Constants ────────────────────────────────────────────────────────────────
NUM_BLOBS      = 7
MAX_DT         = 0.05       # cap dt to avoid tunnelling on first frame
BUOYANCY       = 220.0      # upward acceleration when hot (px/s²)
GRAVITY        = 160.0      # downward acceleration when cool (px/s²)
DRAG           = 0.96       # velocity damping per frame
WALL_REPEL     = 400.0      # lateral repulsion from walls (px/s²)
WALL_MARGIN    = 0.12       # fraction of width to keep blobs away from walls
TEMP_RATE      = 0.6        # how fast temperature tracks target (0–1 blend/s)
TEMP_NOISE     = 0.04       # random walk amplitude on temperature per frame
MERGE_FACTOR   = 1.5        # blob center distance / (r_a + r_b) to start merging
BRIDGE_STEPS   = 6          # translucent circles per bridge

# Warm lava palette (fill colors)
PALETTE = [
    "#ff4d00", "#ff8c00", "#ff2e7a", "#ff6347",
    "#e8003d", "#ff5500", "#cc2200",
]


class Blob:
    __slots__ = ("x", "y", "vx", "vy", "r", "temp", "color")

    def __init__(self, x: float, y: float, r: float, color: str) -> None:
        self.x     = x
        self.y     = y
        self.vx    = 0.0
        self.vy    = 0.0
        self.r     = r
        self.temp  = random.random()   # 0.0 = cold, 1.0 = hot
        self.color = color

    def target_temp(self, h: float) -> float:
        """Blobs near the bottom should be hot; near the top, cold."""
        # y=h → bottom → hot (1.0); y=0 → top → cold (0.0)
        return self.y / h

    def update(self, dt: float, w: float, h: float) -> None:
        """Advance physics by dt seconds."""
        dt = min(dt, MAX_DT)

        # Temperature: blend toward target + small random walk
        target = self.target_temp(h)
        self.temp += (target - self.temp) * TEMP_RATE * dt
        self.temp += random.uniform(-TEMP_NOISE, TEMP_NOISE)
        self.temp  = max(0.0, min(1.0, self.temp))

        # Net vertical acceleration: hot = rise, cold = fall
        # temp=1 → fully hot → buoyancy wins (upward, negative y)
        # temp=0 → fully cold → gravity wins (downward, positive y)
        net_ay = GRAVITY * (1.0 - self.temp) - BUOYANCY * self.temp

        # Lateral wall repulsion keeps blobs off the edges
        margin = w * WALL_MARGIN
        ax = 0.0
        if self.x < margin:
            ax += WALL_REPEL * (1.0 - self.x / margin)
        elif self.x > w - margin:
            ax -= WALL_REPEL * (1.0 - (w - self.x) / margin)

        self.vx += ax * dt
        self.vy += net_ay * dt

        # Viscous drag (lava is thick)
        self.vx *= DRAG
        self.vy *= DRAG

        self.x += self.vx * dt
        self.y += self.vy * dt

        # Hard clamp to bounds (soft bounce off top/bottom)
        if self.y - self.r < 0:
            self.y  = self.r
            self.vy = abs(self.vy) * 0.3
        if self.y + self.r > h:
            self.y  = h - self.r
            self.vy = -abs(self.vy) * 0.3
        self.x = max(self.r, min(w - self.r, self.x))
```

- [ ] **Step 2: Verify no syntax errors**

```bash
python3 -c "import ast; ast.parse(open('examples/lava-lamp/lava_lamp.py').read()); print('OK')"
```

Expected: `OK`

- [ ] **Step 3: Commit**

```bash
cd /Users/ianburke/Documents/GitHub/PLEXI/.claude/worktrees/v3
git add examples/lava-lamp/lava_lamp.py
git commit -m "feat(lava-lamp): Blob class and buoyancy/temperature physics"
```

---

### Task 3: Implement the rendering helpers

**Files:**
- Modify: `examples/lava-lamp/lava_lamp.py`

- [ ] **Step 1: Add `_render_blob` and `_render_bridge` functions**

Add these functions after the `Blob` class (before `if __name__`):

```python
def _hex_to_rgb(h: str) -> tuple[int, int, int]:
    """Parse #rrggbb → (r, g, b)."""
    h = h.lstrip("#")
    return int(h[0:2], 16), int(h[2:4], 16), int(h[4:6], 16)


def _dim_alpha(hex_color: str, alpha: int) -> str:
    """Return hex_color with a given alpha (0–255) as #rrggbbaa."""
    r, g, b = _hex_to_rgb(hex_color)
    return f"#{r:02x}{g:02x}{b:02x}{alpha:02x}"


def _render_blob(ctx: RenderContext, b: Blob) -> None:
    """Draw a blob as layered circles: glow → body → specular highlight."""
    # Outer glow (large, very translucent)
    ctx.circle(b.x, b.y, b.r * 1.55, _dim_alpha(b.color, 22))
    # Mid halo
    ctx.circle(b.x, b.y, b.r * 1.25, _dim_alpha(b.color, 45))
    # Solid core
    ctx.circle(b.x, b.y, b.r, b.color)
    # Specular highlight
    hi_r = max(3.0, b.r * 0.25)
    ctx.circle(b.x - b.r * 0.30, b.y - b.r * 0.30, hi_r, _dim_alpha("#ffffff", 80))


def _render_bridge(ctx: RenderContext, a: Blob, b: Blob, proximity: float) -> None:
    """Draw translucent circles along the line between two nearby blobs.

    proximity: 0.0 = just touching threshold, 1.0 = centers overlapping.
    """
    # Alpha and radius scale with proximity
    base_alpha = int(proximity * 70)
    if base_alpha < 5:
        return

    for i in range(1, BRIDGE_STEPS):
        t   = i / BRIDGE_STEPS
        cx  = a.x + (b.x - a.x) * t
        cy  = a.y + (b.y - a.y) * t
        # Radius: larger in the middle, interpolated between both blob radii
        mid_boost = 1.0 - abs(t - 0.5) * 1.2
        r   = (a.r * (1 - t) + b.r * t) * (0.55 + mid_boost * 0.35)
        # Blend colors by t
        ra, ga, ba_ = _hex_to_rgb(a.color)
        rb, gb, bb_ = _hex_to_rgb(b.color)
        rc  = int(ra + (rb - ra) * t)
        gc  = int(ga + (gb - ga) * t)
        bc  = int(ba_ + (bb_ - ba_) * t)
        col = f"#{rc:02x}{gc:02x}{bc:02x}"
        ctx.circle(cx, cy, r, _dim_alpha(col, base_alpha))
```

- [ ] **Step 2: Verify no syntax errors**

```bash
cd /Users/ianburke/Documents/GitHub/PLEXI/.claude/worktrees/v3
python3 -c "import ast; ast.parse(open('examples/lava-lamp/lava_lamp.py').read()); print('OK')"
```

Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add examples/lava-lamp/lava_lamp.py
git commit -m "feat(lava-lamp): blob and bridge rendering helpers"
```

---

### Task 4: Implement LavaLampApp and wire up the entry point

**Files:**
- Modify: `examples/lava-lamp/lava_lamp.py`

- [ ] **Step 1: Add the App class**

Replace `if __name__ == "__main__": pass` with:

```python
class LavaLampApp(App):

    def on_init(self, ctx: RenderContext) -> None:
        w, h = ctx.w, ctx.h
        self.blobs: list[Blob] = []
        for i in range(NUM_BLOBS):
            r     = random.uniform(22.0, 48.0)
            x     = random.uniform(w * 0.2, w * 0.8)
            y     = random.uniform(h * 0.2, h * 0.9)
            color = PALETTE[i % len(PALETTE)]
            self.blobs.append(Blob(x, y, r, color))

    def on_render(self, ctx: RenderContext) -> None:
        dt = ctx.elapsed
        w, h = ctx.w, ctx.h

        # Physics update
        for blob in self.blobs:
            blob.update(dt, w, h)

        # ── Render ───────────────────────────────────────────────────────────
        ctx.clear("#0a0a1a")

        # Draw merge bridges first (behind blobs)
        blobs = self.blobs
        n = len(blobs)
        for i in range(n):
            for j in range(i + 1, n):
                a, b = blobs[i], blobs[j]
                dx   = b.x - a.x
                dy   = b.y - a.y
                dist = math.sqrt(dx * dx + dy * dy) or 0.001
                threshold = (a.r + b.r) * MERGE_FACTOR
                if dist < threshold:
                    proximity = 1.0 - (dist / threshold)
                    _render_bridge(ctx, a, b, proximity)

        # Draw each blob
        for blob in blobs:
            _render_blob(ctx, blob)

        ctx.emit.schedule_render(16)   # 60 fps

    def on_click(self, ctx: RenderContext, x: float, y: float, button: str) -> None:
        """Inject heat into the nearest blob at the click point."""
        if not self.blobs:
            return
        nearest = min(
            self.blobs,
            key=lambda b: (b.x - x) ** 2 + (b.y - y) ** 2,
        )
        nearest.temp = min(1.0, nearest.temp + 0.5)
        nearest.vy  -= 80.0   # small upward kick


if __name__ == "__main__":
    LavaLampApp().run()
```

- [ ] **Step 2: Verify full file parses cleanly**

```bash
cd /Users/ianburke/Documents/GitHub/PLEXI/.claude/worktrees/v3
python3 -c "import ast; ast.parse(open('examples/lava-lamp/lava_lamp.py').read()); print('OK')"
```

Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add examples/lava-lamp/lava_lamp.py
git commit -m "feat(lava-lamp): LavaLampApp with buoyancy physics and metaball blending"
```

---

### Task 5: Install, smoke-test, and verify

**Files:**
- No new files.

- [ ] **Step 1: Install into v3 app directory**

```bash
cd /Users/ianburke/Documents/GitHub/PLEXI/.claude/worktrees/v3
just install-v3
```

Expected: build succeeds, no compile errors, smoke test passes (PGAP Init → `ready` within 3s, no panics in log).

- [ ] **Step 2: Check app directory**

```bash
ls ~/.plexi-v3/apps/lava-lamp/
```

Expected: `manifest.toml` and `lava_lamp.py` are present.

- [ ] **Step 3: Check executable bit**

```bash
ls -la ~/.plexi-v3/apps/lava-lamp/lava_lamp.py
```

Expected: `-rwxr-xr-x` (executable). If not:

```bash
chmod +x ~/.plexi-v3/apps/lava-lamp/lava_lamp.py
```

- [ ] **Step 4: Tail log for panics**

```bash
tail -30 ~/.plexi-v3/plexi.log
```

Expected: no `PANIC` or `thread 'main' panicked` lines. Look for `app::lava-lamp` ready log.

- [ ] **Step 5: Manual acceptance**

Launch Plexi v3 and open the lava lamp app. Verify:
- Blobs rise and fall without teleporting.
- Blobs that come close show a visible bridge/merge.
- Clicking a blob makes it rise.
- No frozen frame or infinite-loop behavior.

- [ ] **Step 6: Final commit**

```bash
cd /Users/ianburke/Documents/GitHub/PLEXI/.claude/worktrees/v3
git add -p   # review any stray changes
git commit -m "feat(lava-lamp): verified install and smoke test pass"
```
