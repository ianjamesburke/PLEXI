# Stable v1 cut

## V1-blocking

Dependency order. `IMPLEMENT` means close the current contract and prove it. `STUB` means remove or gate the stable surface and return an explicit future-channel message.

| Order | Node | Call | Why | Stints |
|---:|---|---|---|---|
| 1 | `accessibility-tree` | **STUB** | Stable exposes an unqualified AccessKit tree with no screen-reader proof; disable or label it unsupported so nobody mistakes partial semantics for accessibility. | `0702` new |
| 2 | `app-state-scopes` | **IMPLEMENT** | Portable, externally writable state is the open-file boundary behind human and agent use of the same app, and the active chain already targets v1. | `0651` -> `0652` -> `0644` -> `0645` |
| 3 | `pane-state-liveness` | **IMPLEMENT** | A CLI lifecycle value that says `running` after the guest died is a shipped lie on the agent's primary observation path. | `0638` -> `0641`; `0664` -> `0665` |
| 4 | `secrets` | **IMPLEMENT** | Stable's Secrets app can hide keychain values the broker is actively using, which breaks user inspection and the audit promise. | `0441` |
| 5 | `consent-ui` | **IMPLEMENT** | Consent is stable v1's security boundary; model tests do not prove that a user can grant, deny, and remember through the real modal. | `0592` -> `0593` |
| 6 | `wasm-runtime` | **IMPLEMENT** | CPython-in-Wasmtime runs stable Python apps, so prompt guest-death reporting and a zero-skip gate are v1 work even though native WASM distribution is later. | `0638`, `0663`, `0660` |
| 7 | `frame-pacing` | **IMPLEMENT** | Stable ships animated Python apps; two timing paths plus open payload and UI-thread work leave that reachable surface half-qualified. | `0548` -> `0553`; `0549`, `0550`, `0551` |
| 8 | `media-io` | **STUB** | Stable can reach audio and video handlers with mock-only proof; gate those handlers to beta and keep edit-grade decode with the post-v1 editor. | `0703` new; coordinate with `0591` |
| 9 | `release-pipeline` | **IMPLEMENT** | Ian cannot set v1 down while bump, promotion, tag selection, and updater acceptance only have a hand-run round trip. | `0704` new; `0329` is narrower |
| 10 | `ci-gate` | **IMPLEMENT** | A stable release gate cannot carry a known product skip or a temporary two-path bundle switch. | `0663`, `0660` |
| 11 | `core-app-verification` | **IMPLEMENT** | The maintained app set needs one installed-stable sweep proving open, render, primary action, and persistence before v1. | `0590` -> `0602` |

`0701` is a release blocker outside the red/yellow classification: make `ROADMAP.toml` evidence executable in CI and repair the green `daw` claim that fails today.

### Proposed sprint

1. Truth first: `0701`.
2. Stable boundary: `0702`, `0703`, and existing DAW gate `0591`.
3. Product closeouts in parallel: app-state chain, pane-liveness chain, `0441`, consent chain, then WASM and frame-pacing closeouts.
4. Acceptance: zero-skip CI, `0590`/`0602` installed-stable sweep, then `0704` isolated release round trip.

Stop after the acceptance artifacts. Do not pull Assistant, orchestration, browser, marketplace, native WASM distribution, or media editing into this sprint.

## Post-v1

| Node | Why |
|---|---|
| `sandboxing` | NORTH_STAR.md explicitly defines v1 security as consent, audit, and review; true isolation arrives with the later WASM platform phase. |
| `agent-run-orchestration` | The current stable contract stops at agent pane spawn; host-owned workflow execution belongs to Phase 3 and has no misleading stable command today. |
| `mcp-tool-sources` | Assistant and app wrappers are already beta-gated; finish the bridge on beta and remove the dead `[app.mcp]` advertisement there. |
| `wasm-distribution` | Hosted bundle delivery depends on the Phase 4 marketplace. |
| `wasm-cloud-streaming` | Server-side Wasmtime and thin clients belong to the portable-instance phase after distribution. |
| `video-editor` | Timeline, frame-accurate preview, and export are a separate product surface; stable v1 should gate the underlying unqualified media handlers. |
| `browser-surface` | No browser pane exists, so there is no half-working stable surface to clean up. |
| `marketplace-hosted` | Marketplace commands are beta-gated and the public publish path is an explicit stub; Phase 4 owns the full service. |
| `marketplace-monetization` | Accounts, payments, entitlements, and payouts depend on the hosted marketplace and are absent from stable. |
| `biometric-verification` | No biometric promise is exposed in stable; the existing capability broker remains the v1 high-risk-operation boundary. |
| `acp-client` | Hosting third-party coding agents extends the later intelligence phase; named native agent panes are the current contract. |

## Evidence verification

Verdict: `ROADMAP.toml` is not trustworthy yet.

- 63 nodes, 237 evidence entries, 233 unique names.
- 217 Rust entries resolve to real `#[test]` functions. Zero unresolved, zero helper-only matches, zero named evidence tests marked ignored.
- `cargo test --workspace` passed. The main binary reported 2,162 passed, 0 failed, 5 unrelated ignored; all named Rust evidence ran and passed. All crate test binaries also passed.
- 20 scene entries resolve to 19 real scene files. Sixteen unique evidence scenes ran in `scene_suite`; `scaffold-renders-frame1` and `notes-agent-drive` passed when run explicitly because `suite = false`.
- `daw-gate-bundle` resolves but fails twice when run explicitly. Step 6 ends with `input_no_effect`; the run also panics in rfd's macOS async-dialog fallback. The `daw` node is green, so this is a broken green claim.
- No tracked alpha code reads `ROADMAP.toml` or enforces its evidence contract. The full workspace suite misses this failure because `daw-gate-bundle` has `suite = false`.

Scene-backed evidence by node:

| Node | Headless result | Live result |
|---|---|---|
| `contexts` | PASS | UNRUNNABLE: known alpha `scene-live` break |
| `python-sdk` | PASS | UNRUNNABLE: known alpha `scene-live` break |
| `app-lifecycle` | PASS | UNRUNNABLE: known alpha `scene-live` break |
| `event-bus` | PASS | UNRUNNABLE: known alpha `scene-live` break |
| `notifications` | PASS | UNRUNNABLE: known alpha `scene-live` break |
| `assistant` | PASS | UNRUNNABLE: known alpha `scene-live` break |
| `wasm-runtime` | PASS | UNRUNNABLE: known alpha `scene-live` break |
| `ui-kit` | PASS | UNRUNNABLE: known alpha `scene-live` break |
| `text-editor` | PASS | UNRUNNABLE: known alpha `scene-live` break |
| `notes` | PASS | UNRUNNABLE: known alpha `scene-live` break |
| `file-browser` | PASS | UNRUNNABLE: known alpha `scene-live` break |
| `daw` | **FAIL**: `daw-gate-bundle` | UNRUNNABLE: known alpha `scene-live` break |

The live column records the known runner blocker supplied with this audit. No live result was inferred from headless output.

## Local planning state

New tasks: `0701`, `0702`, `0703`, `0704`. Each is `todo`, prioritized, sized, and individually passes `stint check`.

The whole graph still fails `stint check` on five pre-existing status/blocker inconsistencies: `0641`, `0644`, `0645`, `0646`, and `0652`. This audit did not rewrite them.

Open decision for Ian: `.gitignore` keeps `.stint/` local. Choose whether the v1 task graph becomes tracked project state or remains machine-local permanently. The new tasks will not reach git under the current rule.
