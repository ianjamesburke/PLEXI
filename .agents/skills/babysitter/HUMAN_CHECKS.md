# Human Checks — pending validations only a human can do

Appended by the babysitter loop at merge time for stints with a `## Human Check` section (see SKILL.md, "Human-check queue"). Check items off (`- [x]`) as you do them; checked items are pruned at the next run's start. Evidence paths are stable under `.stint/evidence/`.

- [x] PR #2479 (0516: DAW timeline/mixer UI): **DEFERRED INDEFINITELY by Ian 2026-08-01.** The DAW is a development proof of concept to verify the audio pipes, not a product surface. Do not queue DAW UI judgment calls to Ian again — file them as low-priority stints or skip them. open the DAW app in an installed build and judge the timeline visually - lanes, clips, ruler, playhead, mixer legibility. Evidence: .stint/evidence/0516-timeline-ppp1.png and 0516-timeline-ppp2.png. Judging: does the timeline look clean and readable at both scales.
- [ ] Stint 0358 (production hosted-registry install smoke) SKIPPED by the overnight run, untouched, still todo. It is not agent-work: it requires deploying current alpha to PRODUCTION and then doing a real install from a clean machine/profile against the production registry. Deploying production unattended was outside what the run was authorized to do. It is the last unverified step on the free-v1 finish line, so it wants a deliberate slot with you driving.
- [ ] **FAILED 2026-08-01 by Ian — see stint 0693.** The cue is unreachable: `plexi notify` hardcodes priority=50, default interrupt_threshold is 100, so `cue=false` always. Recheck only after 0693 lands. Original entry: PR #2506 (0566: notification choke point + audible cue) MERGED. **THE AUDIBLE CUE WAS NEVER ONCE EMITTED — deliberately, because you were asleep in the same room as this machine all night.** So the one thing 0566 exists to do is the one thing no agent verified. Do this: set `[notifications] sound = "/System/Library/Sounds/Ping.aiff"` in your config, trigger a notification that is actually allowed to interrupt (visible context, focus mode off, priority at or above `interrupt_threshold` — a below-threshold or background-context notification is now *correctly* silent, so testing with one will look like a bug and is not), and confirm you hear it **once**, not clipped short and not repeated. Then confirm the inverse: with `focus_mode = true`, or with the sending context in the background, you hear **nothing**. Judging: is the cue pleasant and correctly timed, and does silence happen exactly when it should. WHY THIS MERGED UNHEARD: both static-review blockers were fixed and I verified each in the code myself; the cue's gating is proven by unit tests that **fail** when the predicate is reverted; and a live tester round measured the whole notification-routing surface with `cue=true count: 0` across the entire session. Note `sound` ships commented out in `scripts/default-config.toml`, so nobody gets an unexpected noise until you opt in. Evidence: PR #2506 body; tester report is transient.
- [x] PR #2506 (0569: notify scope defaults) PASSED by Ian 2026-08-01, notification paints in its own context on switch.
- [ ] (2 of 4 passed: drag-drop + move up/down/top/bottom incl. edge no-ops, Ian 2026-08-01) PR #2505 (0241: subcontexts have no sidebar presence) MERGED on a deliberate, documented partial gate — four checks are not merely unrun, they are **undrivable by any agent with today's tooling**, so they need your eyes once. Open an installed build with at least two master contexts, each holding a subcontext, and confirm: (1) **sidebar drag-drop** a master to a new position, then check `plexi context list` and confirm every context still exists and each child still has its original parent — this is the one that matters, because the failure mode is silent context deletion; (2) **Move Up/Down/Top/Bottom** from the context menu, same inventory check, including Move Up on the first row and Move Down on the last (must be no-ops); (3) **park then unpark** a master that has children — do the children survive and stay attached, and does the parked context stay in the parked section; (4) **Cmd+1..Cmd+9** with subcontexts present — confirm Cmd+N activates the Nth VISIBLE master and not the Nth raw context (the cheapest off-by-one in the PR to get wrong and the easiest to miss by eye). WHY THIS MERGED ANYWAY: no defect was found in anything drivable; Portal descent — now the ONLY route into a subcontext — was verified live PASS across a 4-level descend/ascend chain with all 9 contexts surviving every hop; a static review cleared all 8 named failure candidates with zero blockers; and I independently confirmed the riskiest one myself, that `expand_subtrees` ends with an unseen-index sweep so permutation completeness is total by construction rather than correct-by-enumeration. Zero panics, zero ERROR lines across the whole live session. Judging: does the workspace survive real sidebar manipulation. Evidence: tester report is transient (scratchpad); the durable artifacts are PR #2505's body and the harness test `sidebar_renders_and_numbers_master_contexts_only`.
- [ ] PR #2507 (0570: publish the plexi-cli skill via npx skills) MERGED — and it created a **permanent public repo under your name**: `https://github.com/ianjamesburke/plexi-skills`, installable right now by anyone with `npx skills add ianjamesburke/plexi-skills`. **No leak: I audited it on every push** (tree = exactly `README.md` + `skills/plexi-cli/SKILL.md`; two commits, both touching only those; content scanned for machine/personal markers — nothing from `babysitter/`, `LOG.md`, `RUN_STATE.md`, or any internal workflow skill). It still wants one human read, because it is public and permanent and no agent can judge how it reads to a stranger: skim the README and SKILL.md once as if you were a developer who has only the `plexi` binary. Judging: does this represent the project the way you want it to, publicly and indefinitely. **The one residual worth knowing:** the mirror was **hand-corrected** back to the v0.2.0 surface rather than produced by the new release flow — the `v0.2.0` tag has no `skills/` dir at all, since this PR creates it — so the release gate never validated *this particular* published commit. A tester independently verified it against a real v0.2.0 binary (0 errors), and from the next release onward `promote.sh` closes the loop automatically. Nothing to do about it; just know that the first published copy is the one commit the machine never checked end to end.
- [x] PR #2514 (0564: sidebar context-row status dots): PASSED by Ian 2026-08-01, capsule reads quiet gray, dots crisp, padding fine. look at a sidebar with multi-window dot groups and judge whether the capsule outline now reads as a quiet gray (not attention-grabbing white), the dots look crisp rather than blurry, and the padding has enough air. Also glance at the portal minimap, which shares PANE_DOT_RADIUS. Evidence: none captured — no real scene frame was obtainable overnight (occluded host; scene-live did not complete). Judging: visual taste only; the geometry and stroke changes landed and are unit-gated.
- [x] PR #2514 (0563: paste a URL over a selection wraps it as a markdown link): PASSED by Ian 2026-08-01, wraps correctly, single undo restores. unit-tested (markdown_url_paste_over_selection_wraps_once_and_other_pastes_do_not covers URL-over-selection, non-URL, and no-selection — verified present in the diff), but not drivable live, because host primitives expose text input and not a paste event. Judging: press Cmd+V with a URL over a selection in a real note once and confirm a single undo restores the selection.
- [x] PR #2515 (0562: default soft-wrap in the notes editor): PASSED by Ian 2026-08-01, gutter numbers once per source line, image strips clean. the live tester proved wrap behavior end to end on the installed build (mid-token break, prose space-wrap, visual-row click/Up/Down/Home/End, markdown links, narrow/wide reflow, caret preserved), but the FRAMEBUFFER visual gate could not run -- `just scene-live` is currently broken on alpha, blocked compiling unchanged src/ui/sidebar_row.rs because deprecated egui APIs are promoted to errors. So gutter-number pixels and markdown image-strip visuals over wrapped rows were never seen. Open a note with a long URL and a paragraph plus an image, and glance at it once. Judging: do gutter line numbers appear exactly once per source line (on its first display row, not on every wrapped row), and do image strips sit correctly against wrapped text. Evidence: none captured -- see the scene-live breakage above, which is a separate pre-existing alpha defect, not a 0562 regression.
- [x] PR #2518 (0627: fullscreen terminal input dead): PASSED by Ian 2026-08-01, Cmd+Enter fullscreen, keystrokes reach the shell. after this lands in your next build, full-screen a terminal pane and type — keystrokes must reach the shell. Evidence: falsifiable HostHarness test only (removing the fullscreen input wiring makes it fail with "PTY received []"). Judging: whether the live regression you reported on 2026-07-29 ~01:40 is actually gone. Carve-out reason: the sanctioned CLI exposes no zoom/fullscreen toggle, so no agent can drive this case live (follow-up stint filed).

- [ ] PR #2546 (0674: todo rebuilt on SDK primitives): the todo WRITE tools (todo.add / set_done / remove) invoked from
  the Assistant hit the ask-gate permission dialog, which is out of tester scope by your 2026-08-01 ruling, so no agent
  verified them. Drive it by hand: open todo, ask the assistant to add an item, complete it, and remove it, approving each
  prompt. Judging: do the writes land correctly and does the UI reflect them immediately. Note the read-only tools were
  verified working while the pane is on screen. A SEPARATE off-screen timeout defect was found and traced to a
  pre-existing host defect that reproduces on plain alpha with an app predating this PR, so it was dropped from this
  PR's gate and filed as stint 0688 — every app tool call to a pane that is not rendering times out, todo included.
  Until 0688 lands, drive this check with the todo pane on screen. Evidence: tester report is transient; PR #2546 body.
