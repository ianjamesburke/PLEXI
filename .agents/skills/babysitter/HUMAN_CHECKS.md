# Human Checks — pending validations only a human can do

Appended by the babysitter loop at merge time for stints with a `## Human Check` section (see SKILL.md, "Human-check queue"). Check items off (`- [x]`) as you do them; checked items are pruned at the next run's start. Evidence paths are stable under `.stint/evidence/`.

- [ ] PR #2479 (0516: DAW timeline/mixer UI): open the DAW app in an installed build and judge the timeline visually - lanes, clips, ruler, playhead, mixer legibility. Evidence: .stint/evidence/0516-timeline-ppp1.png and 0516-timeline-ppp2.png. Judging: does the timeline look clean and readable at both scales.
- [x] Local alpha has 2 unpushed commits that blocked `just merge-pr`'s sync gate all night: `645c2dd1 fix(wikipedia): restore search and refresh UI` and `171e2b4b fix(tetris): restore playfield contrast`. They exist nowhere else — not on origin, not in any PR. The overnight loop deliberately did NOT push them and did NOT reset. Decide: push them to alpha, or drop them. Until then every merge in this run will stop at the same gate and need a manual stint closeout.
- [x] PR #2494 (0554: Assistant threat model / authority PRM) is OPEN and DELIBERATELY NOT MERGED — overnight Codex review flagged an architectural gap. The PRM recommends an in-process host reference monitor plus a sandboxed execution helper, explicitly NOT Wasmtime, and the reviewer agrees that call is well argued. Open question for you: the document says the execution helper runs under an "explicit profile" but never names the actual macOS sandbox mechanism (sandbox_init/seatbelt profile? App Sandbox entitlements? something else?). That choice constrains every later implementation stint (0381/0382/0439), so it wants your sign-off before the PRM becomes authoritative. Decide the mechanism, then merge #2494. Reviewer already fixed the doc's factual errors against real code (commits 6f99fea1, 5e5fc075).
- [ ] Stint 0358 (production hosted-registry install smoke) SKIPPED by the overnight run, untouched, still todo. It is not agent-work: it requires deploying current alpha to PRODUCTION and then doing a real install from a clean machine/profile against the production registry. Deploying production unattended was outside what the run was authorized to do. It is the last unverified step on the free-v1 finish line, so it wants a deliberate slot with you driving.
- [x] PR #2495 (0425 test-channel AI key + 0370 calc connector tools) is OPEN and NOT MERGED — a second architectural flag. The overnight Codex reviewer found and FIXED a real security hole first (commit d552ef3b: a production binary renamed `plexi-pr-<N>` could reach the env-var key path and bypass the Keychain; the gate now requires matching compile-time and runtime PR identity). The unresolved question is separate and pre-existing: a connector's `read_only` flag is SELF-DECLARED BY THE APP, and it converts the host's default "Ask" decision into an automatic allow. Explicit host Deny still wins, and calc's own tools are genuinely pure, so nothing here is exploited — but the host never independently establishes tool mutability, and 0370 is the first PR to actually exercise that path with real connector tools. Note this is the same principle 0554's PRM already names ("tool mutability is host policy, not an app connector's self-declared read_only bit"), so deciding it once likely unblocks both PRs. TESTER VERDICT: PASS (validated live at d552ef3b — env-var key path works with no Keychain dialog, unset falls back correctly, calc connector tools work end to end). So #2495 needs nothing but your ruling on the read_only question, then `just merge-pr 2495`.
- [x] PR #2497 (0557: context-scoped kept notes + descendant aggregation) is OPEN and NOT MERGED — third architectural flag. The overnight Codex reviewer first found and FIXED five real defects (commits dcc8f9ae, 60850f08), two of which were genuine DATA-LOSS paths in the note migration: collision retries could duplicate notes (now atomic filename reservation + content verification + fsync before the source is removed), and ambiguous duplicate workspace basenames were being assigned to a context arbitrarily (now left untouched and reported). It also fixed path-component-boundary and symlink handling in descendant matching. The unresolved gap: ROOTLESS CONTEXTS still do not export their effective context path to the CLI, so `plexi note` can capture an empty context_root and a later triage from a different context may file that note under whatever context happens to be active. The code documents this; closing it needs pane-environment plumbing outside this PR's subsystem. Decide whether that gap is acceptable to ship behind, or wants a follow-up stint first. TESTER VERDICT: PASS (validated live at 60850f08 in an ISOLATED temp notes setup — the tester was explicitly barred from touching your real notes: context-scoped save, descendant union, no sibling leak, no ancestor leak, inbox stays global, the /foo/bar vs /foo/barbaz boundary case, and a seeded migration incl. a second run being a no-op). So #2497 needs nothing but your ruling on the rootless-context gap, then `just merge-pr 2497`. Visual evidence for the picker: .stint/evidence/0557-before.png and .stint/evidence/0557-after.png.
- [x] RULED 2026-07-27 by Ian: option (b) — MERGE with the interim posture documented; the OS-sandbox work is now p0 as stint 0577. Original entry follows. PR #2499 (0382: MCP servers bridged onto the connector tool plane) was OPEN, COMPLETE, and DELIBERATELY NOT MERGED — it needs one ruling from you and nothing else. THE BLOCKER: 0554's PRM makes it binding that an untrusted execution helper runs under a seatbelt sandbox (`sandbox_init`, custom profile). The MCP bridge launches an external, user-configured MCP server subprocess — exactly that case — with a raw `Command`, so the server gets ambient filesystem and network authority before any connector consent. The worker PROVED, rather than assumed, that the seatbelt helper does not exist in-tree: `rg -i 'sandbox_init|seatbelt|sandbox-exec'` across src/, sdk/ and scripts/ matches exactly ONE file, docs/assistant-authority-model.md — the PRM itself, which says the profile is "deliberately a separate follow-on". There is no sandbox helper module, no worker binary, no sandboxed-spawn path anywhere. The closest primitive is build_exec.rs's process-group spawn, which is containment, not sandboxing. I explicitly forbade the worker from building one, because designing Plexi's first OS-level sandbox is your call, not an unattended agent's. YOUR RULING, one of two: (a) park #2499 until a 0554 seatbelt worker exists, or (b) accept "user-configured command plus process-group containment, no OS sandbox" as the explicit interim posture for MCP servers — in which case #2499 merges as-is. Everything else the reviewer raised was fixed (commits 3298d287, a6d297b1, 471c1ff7): the connection type is now structurally incapable of unbounded operation (process-group teardown with a take-once pgid so a recycled pid can't be signalled, bounded inbound channel, line-capped stderr, non-blocking writer thread, a 120s host first-output watchdog plus a 90s app-side handshake timer). Gate green: 1910 passed / 0 failed, mcp_client 17/17, bridge tests 24, mypy clean, check-docs green, all 6 PR checks pass. Residual documented honestly in the PR: a descendant that calls setsid escapes the process group — closing that is precisely the missing 0554 worker. NOTE the PR body already states the interim posture plainly, so option (b) is not an undocumented compromise.
